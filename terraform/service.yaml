apiVersion: v1 
kind: Service
metadata:
  name: nodejs-service
  labels:
    app: nodejs
spec:
  selector:
    app: nodejs
  ports:
    - name : http 
      port: 3000
      protocol: TCP
      targetPort: 3000
  type: LoadBalancer
